# レーダーチャートのグリッド表示改善

## 🐛 問題

レーダーチャートの枠（グリッド線）が複数重なって表示されていました。

### 従来の表示
```
   5段階のグリッド
   ┌─────┐
   │┌───┐│
   ││┌─┐││  ← 複数の枠が重なって見づらい
   │││●│││
   ││└─┘││
   │└───┘│
   └─────┘
```

## ✅ 解決方法

### デフォルト: シンプルスタイル（外枠のみ）

```
   外枠のみ
   ┌─────┐
   │     │
   │  ●  │  ← すっきり見やすい！
   │     │
   └─────┘
```

### オプション: 詳細スタイル（複数レベル）

必要に応じて、5段階のグリッドを表示可能。

## 🔧 実装内容

### 1. RadarGridView の改善

**変更点:**
- `showMultipleLevels`パラメータを追加
- デフォルトは`false`（外枠のみ）
- `true`にすると5段階のグリッドを表示

```swift
struct RadarGridView: View {
    let points: Int
    let size: CGFloat
    let showMultipleLevels: Bool
    
    init(points: Int, size: CGFloat, showMultipleLevels: Bool = false) {
        self.points = points
        self.size = size
        self.showMultipleLevels = showMultipleLevels
    }
    
    var body: some View {
        ZStack {
            if showMultipleLevels {
                // 複数のグリッドレベル（5段階）
                ForEach(1...5, id: \.self) { level in
                    RadarPolygon(points: points, size: size * CGFloat(level) / 5.0)
                        .stroke(Color.gray.opacity(0.15), lineWidth: 1)
                }
            } else {
                // 外枠のみ
                RadarPolygon(points: points, size: size)
                    .stroke(Color.gray.opacity(0.3), lineWidth: 1.5)
            }
            
            // 中心から各頂点への線
            ForEach(0..<points, id: \.self) { index in
                // ...
            }
        }
    }
}
```

### 2. RadarChartView の更新

**追加パラメータ:**
```swift
init(
    data: [RadarDataPoint],
    size: CGFloat = 200,
    showLabels: Bool = true,
    showMultipleLevels: Bool = false  // 新規追加
)
```

## 📊 使用方法

### シンプルスタイル（推奨・デフォルト）

```swift
RadarChartView(
    data: avatarStats.getRadarData(),
    size: 200
    // showMultipleLevels省略（= false）
)
```

**表示:**
- 外枠のみ（太さ1.5pt、不透明度30%）
- 中心から各頂点への線
- すっきりとした見た目

### 詳細スタイル

```swift
RadarChartView(
    data: avatarStats.getRadarData(),
    size: 200,
    showMultipleLevels: true  // 明示的に有効化
)
```

**表示:**
- 5段階の同心グリッド（不透明度15%）
- 各レベルで進捗を確認可能
- より詳細な情報

## 🎨 デザインの違い

### 外枠のみ（デフォルト）

**メリット:**
- ✅ すっきりとした見た目
- ✅ データが見やすい
- ✅ モダンなデザイン
- ✅ 初心者にも分かりやすい

**用途:**
- ホーム画面
- 一覧表示
- 概要確認

### 複数レベル

**メリット:**
- ✅ 詳細な進捗確認
- ✅ レベル感が分かりやすい
- ✅ 比較がしやすい

**用途:**
- アバター詳細画面
- 統計画面
- 詳細分析

## 🎯 視覚的な比較

### 従来（5段階グリッド）

```
         腕
        ／＼
     ××●××● 肩
     ×××××
   足 ×●×××
     ×××××
    背筋─腹筋

× = グリッド線（5段階分）
● = データポイント

→ 線が多すぎて見づらい ❌
```

### 改善後（外枠のみ）

```
         腕
        ／＼
      ／  ●＼ 肩
     ／   │  ＼
   足 ●───┼───●
     ＼   │  ／
      ＼ ●／
       背筋─腹筋

│ ─ = 頂点への線
外枠 = 1本のみ

→ すっきり見やすい ✅
```

## 🔍 技術的な詳細

### グリッドの不透明度調整

```swift
// 複数レベルの場合
.stroke(Color.gray.opacity(0.15), lineWidth: 1)
// → 薄くして目立たないように

// 外枠のみの場合
.stroke(Color.gray.opacity(0.3), lineWidth: 1.5)
// → 少し濃く、太くしてしっかり表示
```

### 線の太さの違い

| スタイル | 線の太さ | 不透明度 |
|---------|---------|---------|
| 複数レベル | 1.0pt | 15% |
| 外枠のみ | 1.5pt | 30% |

## 📱 各画面での使用

### HomeView.swift

```swift
// シンプルスタイル（デフォルト）
RadarChartView(
    data: appData.avatarStats.getRadarData(),
    size: 200,
    showLabels: true
    // showMultipleLevels: false（デフォルト）
)
```

### AvatarDetailView.swift

必要に応じて詳細スタイルに切り替え可能：

```swift
// 詳細スタイル（オプション）
RadarChartView(
    data: appData.avatarStats.getRadarData(),
    size: 280,
    showLabels: true,
    showMultipleLevels: true  // 詳細画面では複数レベルもあり
)
```

## 🧪 テスト

### Previewでの確認

両方のスタイルを並べて表示：

```swift
#Preview {
    ScrollView {
        VStack(spacing: 30) {
            // シンプルスタイル
            VStack {
                Text("シンプルスタイル（外枠のみ）")
                RadarChartView(
                    data: sampleData,
                    showMultipleLevels: false
                )
            }
            
            Divider()
            
            // 詳細スタイル
            VStack {
                Text("詳細スタイル（複数レベル）")
                RadarChartView(
                    data: sampleData,
                    showMultipleLevels: true
                )
            }
        }
    }
}
```

### アプリでの確認

1. アプリを起動
2. ホーム画面を確認
   - ✅ 外枠のみが表示される
   - ✅ すっきりした見た目
   - ✅ データが見やすい

3. アバター詳細画面を開く
   - ✅ 必要に応じて複数レベルも表示可能

## 💡 デザインガイドライン

### いつシンプルスタイルを使うか

- ✅ 小さいサイズのチャート（< 250pt）
- ✅ 一覧表示
- ✅ ダッシュボード
- ✅ 概要確認

### いつ詳細スタイルを使うか

- ✅ 大きいサイズのチャート（> 280pt）
- ✅ 詳細画面
- ✅ 分析画面
- ✅ レベル感を強調したい場合

## 🎨 カスタマイズ例

### グリッドレベル数の変更

```swift
// 3段階のグリッド
ForEach(1...3, id: \.self) { level in
    RadarPolygon(points: points, size: size * CGFloat(level) / 3.0)
        .stroke(Color.gray.opacity(0.15), lineWidth: 1)
}
```

### カラーのカスタマイズ

```swift
// テーマカラーに合わせる
if showMultipleLevels {
    ForEach(1...5, id: \.self) { level in
        RadarPolygon(points: points, size: size * CGFloat(level) / 5.0)
            .stroke(Color.blue.opacity(0.1), lineWidth: 1)  // 青系
    }
} else {
    RadarPolygon(points: points, size: size)
        .stroke(Color.blue.opacity(0.4), lineWidth: 2)  // 太い青枠
}
```

### アニメーション付き切り替え

```swift
struct RadarChartView: View {
    @State private var showMultipleLevels = false
    
    var body: some View {
        ZStack {
            RadarGridView(
                points: data.count,
                size: size,
                showMultipleLevels: showMultipleLevels
            )
            .animation(.easeInOut, value: showMultipleLevels)
            
            // タップで切り替え
        }
        .onTapGesture {
            showMultipleLevels.toggle()
        }
    }
}
```

## ✅ 結果

- ✅ **外枠のみがデフォルト**
- ✅ **すっきりとした見た目**
- ✅ **必要に応じて詳細表示可能**
- ✅ **柔軟なカスタマイズ**
- ✅ **ユーザー体験の向上**

チャートが見やすくなり、ユーザーは自分のステータスを一目で理解できます！📊✨
