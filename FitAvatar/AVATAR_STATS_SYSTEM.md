# 部位別ステータスシステムの実装

## ✅ 実装完了した機能

### 🎯 アバター部位別ステータスシステム

従来の単一経験値システムから、体の部位ごとにステータスを管理する新しいシステムに変更しました。

## 📊 システム概要

### 5つの部位ステータス

| 部位 | アイコン | 色 | 関連トレーニング |
|-----|---------|---|----------------|
| 腕 (arms) | figure.arms.open | 青 | プッシュアップ、ベンチプレス、懸垂 |
| 肩 (shoulders) | figure.arms.open | 紫 | プッシュアップ、ベンチプレス |
| 腹筋 (abs) | figure.core.workout | オレンジ | プランク、クランチ、有酸素運動 |
| 背筋 (back) | figure.strengthtraining.traditional | 緑 | 懸垂、デッドリフト |
| 足 (legs) | figure.walk | 赤 | スクワット、ランジ、有酸素運動 |

### レベルシステム

```
100 XP = 1レベル

例：
- 150 XP → レベル2（次のレベルまで50 XP）
- 300 XP → レベル4（次のレベルまで100 XP）
- 999 XP → レベル10（次のレベルまで1 XP）
```

## 🎨 新しいUIコンポーネント

### 1. レーダーチャート（RadarChartView）

**機能:**
- 5つの部位のステータスを視覚的に表示
- 五角形のグラフ
- グリッド線とラベル
- インタラクティブな表示

**使用例:**
```swift
RadarChartView(
    data: appData.avatarStats.getRadarData(),
    size: 200,
    showLabels: true
)
```

### 2. アバター詳細画面（AvatarDetailView）

**表示内容:**
- レーダーチャート（大きいサイズ）
- 総合レベル
- 部位別ステータス一覧
  - 各部位のレベル
  - 現在のXP
  - 次のレベルまでのXP
  - プログレスバー

### 3. ホーム画面の統合

**変更点:**
- アバターセクションにレーダーチャート表示
- 総合レベル表示
- タップでアバター詳細画面へ遷移

## 🏗️ データ構造

### AvatarStats

```swift
struct AvatarStats: Codable {
    var arms: Int       // 腕のXP
    var shoulders: Int  // 肩のXP
    var abs: Int        // 腹筋のXP
    var back: Int       // 背筋のXP
    var legs: Int       // 足のXP
    
    // 総合レベル
    var overallLevel: Int {
        (arms + shoulders + abs + back + legs) / 5
    }
    
    // 部位ごとのレベル計算
    func getLevel(for stat: BodyPart) -> Int
    
    // レーダーチャート用データ
    func getRadarData() -> [RadarDataPoint]
}
```

### BodyPart

```swift
enum BodyPart: String, CaseIterable {
    case arms = "腕"
    case shoulders = "肩"
    case abs = "腹筋"
    case back = "背筋"
    case legs = "足"
}
```

## 🔄 トレーニングとステータスのマッピング

### Exercise → BodyPart変換

```swift
extension Exercise {
    var trainedBodyParts: [BodyPart] {
        switch category {
        case .upperBody:
            if name.contains("プッシュアップ") || name.contains("ベンチプレス") {
                return [.arms, .shoulders]  // 腕と肩を鍛える
            } else if name.contains("懸垂") {
                return [.arms, .back]        // 腕と背筋を鍛える
            } else {
                return [.arms, .shoulders]
            }
            
        case .lowerBody:
            return [.legs]                   // 足を鍛える
            
        case .core:
            return [.abs]                    // 腹筋を鍛える
            
        case .cardio:
            return [.legs, .abs]             // 足と腹筋を鍛える
        }
    }
}
```

## 💾 データの永続化

### UserDefaultsへの保存

```swift
// AppData.swift

// 保存
private func saveAvatarStats() {
    if let encoded = try? JSONEncoder().encode(avatarStats) {
        UserDefaults.standard.set(encoded, forKey: "avatarStats")
    }
}

// 読み込み
private func loadAvatarStats() {
    if let data = UserDefaults.standard.data(forKey: "avatarStats"),
       let decoded = try? JSONDecoder().decode(AvatarStats.self, from: data) {
        avatarStats = decoded
    }
}
```

## 🎮 使用フロー

### トレーニング完了時

```
1. ユーザーがトレーニングを完了
   ↓
2. XPを計算（基本XP × セット数 × 難易度）
   例: 15 × 3セット × 2（中級） = 90 XP
   ↓
3. トレーニングの種類から鍛えられる部位を判定
   例: プッシュアップ → [腕, 肩]
   ↓
4. 各部位にXPを追加
   腕: +90 XP
   肩: +90 XP
   ↓
5. データを保存
   ↓
6. ホーム画面のレーダーチャートが更新される
```

### 実装コード

```swift
// WorkoutRecordView.swift

private func completeWorkout() {
    // XP計算
    let totalXP = baseXP * setMultiplier * difficultyMultiplier
    
    // ワークアウト記録を保存
    appData.addWorkout(record)
    
    // 部位別ステータスにXPを追加
    let bodyParts = exercise.trainedBodyParts
    appData.addXPToStats(totalXP, for: bodyParts)
    
    // 完了
    showingCompletionAlert = true
}
```

## 📊 レーダーチャートの仕組み

### 描画ロジック

1. **グリッド線の描画**
   - 5つの頂点を持つ五角形
   - 5段階のレベル（同心円状）
   - 中心から各頂点への線

2. **データポリゴンの描画**
   - 各部位のXPを正規化（0.0〜1.0）
   - 最大値で除算して相対値を計算
   - 5つの点を結んでポリゴンを作成

3. **ラベルの配置**
   - 各頂点の外側に部位名を配置
   - 角度計算: `2π / 5 × index - π / 2`

### 数学的な計算

```swift
// 角度計算（上から時計回り）
let angle = 2 * .pi / Double(points) * Double(index) - .pi / 2

// 座標計算
let x = center.x + radius * cos(angle)
let y = center.y + radius * sin(angle)
```

## 🎨 UIの改善点

### プログレスバーのデザイン

```swift
// グラデーション付きプログレスバー
RoundedRectangle(cornerRadius: 8)
    .fill(
        LinearGradient(
            gradient: Gradient(colors: [colorForPart.opacity(0.7), colorForPart]),
            startPoint: .leading,
            endPoint: .trailing
        )
    )
    .frame(width: geometry.size.width * progress, height: 12)
```

### 色分け

各部位に固有の色を設定：
- **腕**: 青（Blue）
- **肩**: 紫（Purple）
- **腹筋**: オレンジ（Orange）
- **背筋**: 緑（Green）
- **足**: 赤（Red）

## 🚀 今後の拡張可能性

### 1. 3Dアバターの統合

```swift
// 部位のレベルに応じてアバターの外見を変更
func getAvatarMuscleSize(for part: BodyPart) -> Float {
    let level = avatarStats.getLevel(for: part)
    return 1.0 + Float(level) * 0.1  // レベル1増加で10%大きくなる
}
```

### 2. 特殊能力のアンロック

```swift
// 特定のレベルに達すると能力解放
func getUnlockedAbilities() -> [Ability] {
    var abilities: [Ability] = []
    
    if avatarStats.getLevel(for: .arms) >= 10 {
        abilities.append(.powerPunch)
    }
    
    if avatarStats.getLevel(for: .legs) >= 15 {
        abilities.append(.highJump)
    }
    
    return abilities
}
```

### 3. バランスボーナス

```swift
// 全ての部位が均等に鍛えられているとボーナス
func getBalanceBonus() -> Int {
    let levels = BodyPart.allCases.map { avatarStats.getLevel(for: $0) }
    let maxLevel = levels.max() ?? 0
    let minLevel = levels.min() ?? 0
    let difference = maxLevel - minLevel
    
    if difference <= 2 {
        return 100  // バランスボーナス！
    }
    
    return 0
}
```

### 4. リーダーボード

```swift
// 部位別ランキング
struct LeaderboardEntry {
    let userName: String
    let part: BodyPart
    let level: Int
    let rank: Int
}
```

### 5. チャレンジシステム

```swift
// 特定の部位を集中的に鍛えるチャレンジ
struct Challenge {
    let title: String
    let targetPart: BodyPart
    let targetXP: Int
    let reward: Int
    let deadline: Date
}
```

## 🧪 テスト方法

### 1. 基本動作確認

1. アプリを起動
2. ホーム画面でレーダーチャートを確認
3. アバターセクションをタップ
4. アバター詳細画面が表示される
5. 各部位のステータスを確認

### 2. XP獲得テスト

1. ワークアウトタブを開く
2. プッシュアップを選択
3. トレーニングを記録（3セット）
4. 完了
5. ホーム画面に戻る
6. レーダーチャートで「腕」と「肩」が増えている ✅

### 3. 複数トレーニングのテスト

```
プッシュアップ (3セット) → 腕+90, 肩+90
スクワット (4セット)     → 足+120
プランク (3セット)       → 腹筋+90
懸垂 (2セット, 上級)     → 腕+90, 背筋+90
```

期待される結果：
- 腕: 180 XP (レベル2)
- 肩: 90 XP (レベル1)
- 腹筋: 90 XP (レベル1)
- 背筋: 90 XP (レベル1)
- 足: 120 XP (レベル2)
- 総合レベル: 1

## 📁 新規作成ファイル

1. **AvatarStats.swift**
   - アバターステータスモデル
   - 部位の定義
   - レベル計算ロジック

2. **RadarChartView.swift**
   - レーダーチャートの描画
   - グリッド線
   - データポリゴン
   - ラベル

3. **AvatarDetailView.swift**
   - アバター詳細画面
   - 部位別ステータス表示
   - プログレスバー

## 🔧 変更されたファイル

1. **AppData.swift**
   - `avatarStats`プロパティ追加
   - 保存/読み込みメソッド
   - `addXPToStats()`メソッド

2. **WorkoutRecordView.swift**
   - 完了時に部位別XP追加

3. **HomeView.swift**
   - レーダーチャート表示
   - アバター詳細へのナビゲーション

4. **Exercise.swift**
   - `trainedBodyParts`プロパティ追加

## 🎉 まとめ

この実装により、ユーザーは：

- ✅ **トレーニングの効果を視覚的に確認**できる
- ✅ **バランスよく体を鍛える**動機付けができる
- ✅ **部位ごとの成長を追跡**できる
- ✅ **レーダーチャートで一目で現状把握**できる
- ✅ **ゲーム要素が増えて楽しい**

より詳細でエンゲージメントの高いフィットネスアプリが完成しました！🎮💪
